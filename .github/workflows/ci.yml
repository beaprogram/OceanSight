name: Pipeline checks
on: [push, pull_request]
jobs:
  checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
          cache: pip
      - run: pip install -r requirements-inference-lock.txt -r requirements-dev.txt
      - run: ruff check oceansight app.py tests
      - run: pytest -q
        # Local real-data/model tests skip when copyrighted artifacts are absent.
